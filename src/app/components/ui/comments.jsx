import React from "react";
import PropTypes from "prop-types";

const Comments = ({ user }) => {
  console.log(user);
  return <h1>sdfsdf</h1>;
};

Comments.propTypes = {
  data: PropTypes.array
};
export default Comments;
